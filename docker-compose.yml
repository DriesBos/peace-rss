services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: unless-stopped

  miniflux:
    image: miniflux/miniflux:latest
    depends_on:
      - postgres
    environment:
      DATABASE_URL: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?sslmode=disable
      BASE_URL: ${MINIFLUX_PUBLIC_URL}
      RUN_MIGRATIONS: '1'
      # Admin bootstrap (optional if already created)
      CREATE_ADMIN: '1'
      ADMIN_USERNAME: ${MINIFLUX_ADMIN_USERNAME}
      ADMIN_PASSWORD: ${MINIFLUX_ADMIN_PASSWORD}
    restart: unless-stopped

  rss-bridge:
    image: rssbridge/rss-bridge:latest
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      args:
        NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY}
    environment:
      NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY}
      CLERK_SECRET_KEY: ${CLERK_SECRET_KEY}
      MINIFLUX_BASE_URL: ${MINIFLUX_BASE_URL}
      MINIFLUX_ADMIN_USERNAME: ${MINIFLUX_ADMIN_USERNAME}
      MINIFLUX_ADMIN_PASSWORD: ${MINIFLUX_ADMIN_PASSWORD}
      RSS_BRIDGE_BASE_URL: ${RSS_BRIDGE_BASE_URL}
      SOCIAL_FEEDS_BASE_URL: ${SOCIAL_FEEDS_BASE_URL}
      SOCIAL_FEED_TOKEN_SECRET: ${SOCIAL_FEED_TOKEN_SECRET}
      SOCIAL_METRICS_TOKEN: ${SOCIAL_METRICS_TOKEN}
      SOCIAL_CREATE_RATE_LIMIT_PER_MINUTE: ${SOCIAL_CREATE_RATE_LIMIT_PER_MINUTE}
      SOCIAL_PROXY_GLOBAL_RATE_LIMIT_PER_MINUTE: ${SOCIAL_PROXY_GLOBAL_RATE_LIMIT_PER_MINUTE}
      SOCIAL_PROXY_SOURCE_RATE_LIMIT_PER_MINUTE: ${SOCIAL_PROXY_SOURCE_RATE_LIMIT_PER_MINUTE}
      SOCIAL_PROXY_CACHE_TTL_MS: ${SOCIAL_PROXY_CACHE_TTL_MS}
      SOCIAL_DISCOVERY_CACHE_TTL_MS: ${SOCIAL_DISCOVERY_CACHE_TTL_MS}
    depends_on:
      - miniflux
      - rss-bridge
    restart: unless-stopped

  caddy:
    image: caddy:2-alpine
    ports:
      - '80:80'
      # If you want https locally later, add 443 back. For now, keep it off.
      # - '443:443'
      # - '443:443/udp'
    volumes:
      - ./infra/proxy/Caddyfile.local:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - frontend
      - miniflux
    restart: unless-stopped

volumes:
  pgdata:
  caddy_data:
  caddy_config:
